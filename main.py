jmeno = "Radim" #Proměnná se stringem (mým jménem).
prijmeni = " Kočko" #Proměnná se stringem (mým příjmením).
print(jmeno + prijmeni) #Vypíše proměnné do konzole.
vek = int(input("Kolik je vám let?: "))
print("Je vám: ", + vek, "let.")
def vek():
    while True:
        vek
        if vek.isdigit():
            print("Okay")
            break
        else:
            print("Zadej svůj věk číselnou hodnotou!")